import * as EMComponents from '@ui/em-design-system';
import * as EMSections from '@ui/sections';
import * as Pages from './app/pages';

console.log(`${typeof Pages} :: ${typeof EMComponents} :: ${typeof EMSections}`);
